import Button from "../utilities/Button"

const TaskAddButton = () => {
    return (
        <Button>
            <span className="plus-icon">+</span>
            <span>Task</span>
        </Button>
    )
}

export default TaskAddButton